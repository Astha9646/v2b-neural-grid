# =============================================================================
# V2B Neural Grid — production dependencies (CPU inference + FastAPI API)
# Python 3.11+ recommended
#
# Install order (Docker / Railway / bare metal):
#   pip install --upgrade pip
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-production.txt
#
# Torch is installed separately because the CPU wheel lives on PyTorch's index,
# not PyPI. Dev environments may use torch 2.12.x; 2.5.1+ is sufficient for RL
# Actor inference in production.
# =============================================================================

# --- API server ---
fastapi==0.136.1
uvicorn==0.47.0
httptools==0.8.0
websockets==16.0
python-multipart==0.0.29

# --- Settings & validation ---
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# --- Auth ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# --- Database ---
SQLAlchemy==2.0.49

# --- RL inference & telemetry ---
numpy==2.4.6
pandas==3.0.3
gymnasium==1.2.3

# --- Observability ---
psutil==7.2.2

# --- Enterprise report exports (PDF) ---
fpdf2==2.8.3
